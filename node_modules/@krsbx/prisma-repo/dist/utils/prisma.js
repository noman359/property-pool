"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPrismaLogger = void 0;
const constants_1 = require("./constants");
const convertToString = (value) => `[${value.map((type) => `'${type}'`).join(', ')}]`;
const getPrismaLogger = (config) => {
    const loggerSettings = [];
    if (Boolean(config.error) === true)
        loggerSettings.push(constants_1.PRISMA_LOGGER.ERROR);
    if (Boolean(config.info) === true)
        loggerSettings.push(constants_1.PRISMA_LOGGER.INFO);
    if (Boolean(config.query) === true)
        loggerSettings.push(constants_1.PRISMA_LOGGER.QUERY);
    if (Boolean(config.warn) === true)
        loggerSettings.push(constants_1.PRISMA_LOGGER.WARN);
    return convertToString(loggerSettings);
};
exports.getPrismaLogger = getPrismaLogger;
//# sourceMappingURL=prisma.js.map